"use strict";
// lol i guess if a field is undefined it uses the default

const defaultguildsettings = {
   name: "guildsettings"
};
const defaultusersettings = {
   name: "usersettings"
};

module.exports = {
   defaultguildsettings: defaultguildsettings,
   defaultusersettings: defaultusersettings
};
